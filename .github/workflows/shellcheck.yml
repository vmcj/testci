name: 'Shellcheck'

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches:
      - main

jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
      - run: echo "Always run this check"
